apiVersion: v1
kind: Service
metadata:
  name: mcc-security-service
  namespace: stech
spec:
  selector:
    app: mcc-security-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP