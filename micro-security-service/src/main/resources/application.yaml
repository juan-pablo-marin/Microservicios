spring:
  application:
    name: micro-security-service
mcc-security:
  applications:
    - clientId: web01
      clientSecret: "{noop}secretweb"
settings:
  issuer-uri: ${URL_SECURITY_SERVICE:http://localhost:9000}
server:
  port: ${SERVER_PORT:9000}